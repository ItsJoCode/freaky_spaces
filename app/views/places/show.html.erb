<div class="container">
  <div class="row justify-content-center">
    <h1>Detail of <%= @place.name %></h1>
    <h2>Owner : <%= @place.user.first_name %> <%= @place.user.last_name %></h2>
    <p><%= @place.description %></p>
    <p><%= @place.address %></p>
    <p>Capicity : <%= @place.capacity %> | $<%= @place.price_per_day %>/day</p>
  </div>
</div>
